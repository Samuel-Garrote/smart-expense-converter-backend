server:
  port: ${PORT:8080}
spring:
  application:
    name: smart-expense-converter
  ai:
    google:
      genai:
        api-key: ${GOOGLE_API_KEY}
        chat:
          options:
            model: gemini-3.6-flash
app:
  cors:
    allowed-origin: http://localhost:4200
